# /etc/pam_radius_auth.conf
# PAM RADIUS client — points to cam_radius Docker container
# shared secret must match docker/radius/freeradius/clients.conf
#
# Install:
#   sudo cp system-config/radius/pam_radius_auth.conf /etc/pam_radius_auth.conf
#   sudo chmod 600 /etc/pam_radius_auth.conf

# server:port    shared_secret    timeout(s)
127.0.0.1:1812   testing123       3
